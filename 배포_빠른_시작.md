# 🚀 5분 만에 배포하기 - 빠른 시작 가이드

## ⚡ 초고속 배포 (Render)

### 1단계: GitHub 업로드 (2분)

```bash
# 1. GitHub 저장소 생성 (웹에서)
#    https://github.com/new

# 2. 터미널에서 실행
git init
git add .
git commit -m "Deploy: Financial Statement App"
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git
git branch -M main
git push -u origin main
```

### 2단계: Render 배포 (3분)

1. https://render.com 접속 → GitHub로 가입
2. **"New +"** → **"Web Service"**
3. 저장소 선택 → **"Connect"**

#### 설정 입력:

**Name**: `financial-app` (원하는 이름)

**Build Command**: 
```bash
pip install -r requirements.txt && python download_corp_code.py && python init_db.py
```

**Start Command**:
```bash
gunicorn app:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120
```

#### 환경 변수 (Environment):

| Key | Value |
|-----|-------|
| `OPENDART_API_KEY` | `당신의_OpenDart_키` |
| `GEMINI_API_KEY` | `당신의_Gemini_키` |
| `PYTHONIOENCODING` | `utf-8` |

4. **"Create Web Service"** 클릭

### 3단계: 완료! ✅

5-10분 후 URL 확인:
```
https://YOUR-APP-NAME.onrender.com
```

---

## 🎯 체크리스트

배포 전:
- [ ] GitHub 저장소 생성
- [ ] 코드 푸시 완료
- [ ] API 키 준비

Render 설정:
- [ ] Build Command 입력
- [ ] Start Command 입력
- [ ] 환경 변수 3개 설정

테스트:
- [ ] URL 접속
- [ ] 회사 검색
- [ ] AI 설명 생성

---

## 💡 문제 해결

### Build Failed?
→ Build Command 다시 확인

### API Key Error?
→ 환경 변수에서 따옴표 제거

### Database Error?
→ Build Command에 `init_db.py` 있는지 확인

---

## 📞 상세 가이드

더 자세한 내용은 `배포_완벽_가이드.md` 참고!

**Happy Deploying! 🎉**

